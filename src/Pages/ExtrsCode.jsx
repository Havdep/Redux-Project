{
  /* <div className="mt-16 px-8  flex flex-wrap justify-around ">
      {products.map((prod) => (
        <div key={prod.id} className="mb-8 p-4 bg-gray-100 rounded-lg w-72">
          <img
            src={prod.image}
            alt=""
            className="w-40 h-40 justify-center items-center mb-4 rounded-lg"
          />
          <h4 className="text-xl font-semibold mb-2">{prod.title}</h4>
          <h4 className="text-lg mb-2">${prod.price}</h4>
          <button className="bg-indigo-500 text-white px-4 py-2 rounded-lg hover:bg-indigo-600">
            Add to Cart
          </button>
        </div>
      ))}
    </div> */
}
